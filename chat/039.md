完成正式 continuation 前的最小修正；未改 physics、solver、checkpoint、restart semantics 或 workflow architecture，也未啟動完整 420→1800 s run。

- `run/01_ADR01/main.py`: `run()` 現在只回傳既有 result，不再替 Python caller 印出整份 observations JSON；direct CLI path 改為接住 result 後再 `print(json.dumps(result))`，CLI 行為保留。
- `run/01_ADR01/restart.py`: child manifest 的 `parent_manifest` 與 `restart_checkpoint` 改為 `output/baseline-v1/...` 的 case-relative POSIX path；新增由 parent manifest 直接繼承的 `parent_git_dirty`，原有 `parent_git_commit` 保留。
- `handoff.md`: 記錄上述 pre-launch 狀態及 production child 尚未執行、人工 review 後可啟動。

短 regression 結果：Python compile、runtime-log self-test、workflow self-test 全部 PASS；420→424 s restart continuity PASS，first/final progress 與 `run.log` 正常。Python caller 輸出只有 runtime logging，沒有 result JSON；單步 direct CLI 仍輸出 result JSON。Child manifest 的兩個 lineage path 均為 relative，parent commit/dirty 皆存在，`validation_status = NOT_PERFORMED`、`validated_through_s = 420`。Parent manifest/checkpoint/summary 的 Git blob hash 前後一致。兩個 temporary regression output 已刪除。

狀態：`baseline-v1-cont-420s-1800s` 已 ready，等待人工確認後再正式啟動。
