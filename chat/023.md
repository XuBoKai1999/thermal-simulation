# Documentation maintenance — IC audit split into Step 8A and Step 13

## Completed

- Renamed the unnumbered validation gate to `Step 8A — Initial-condition
  representation baseline audit`.
- Limited Step 8A to checks that do not require a mesh sweep:
  intended `by_region` IC versus actual FE t=0 field, t=0 per-region
  Tmin/Tmax/Tavg, ideal-textual versus actual-FE initial stored-energy difference,
  initial interface heat-flux magnitude, and mandatory use of the actual solver
  t=0 field in later energy-balance calculations.
- Moved mesh-dependent IC representation questions to Step 13 Mesh convergence.
  Step 13 must compare initial region statistics, stored-energy discrepancy,
  initial interface flux, and whether those discrepancies decrease with refined
  measured `h_char`.
- Removed the stale manual statement that ADR01 baseline had not started; the
  manual now records the completed Baseline v1 early-transient smoke run and root
  `steps2.md` roadmap.
- Updated the manual's Test 07 description to include semantic `by_region` IC.

## Files changed

- `steps2.md` — Step 8A scope and Step 13 mesh-refinement checks.
- `manual/07-feature-status.md` — current ADR01 status and Test 07 coverage.
- `chat/023.md` — this handoff.

## Tests

- No simulation or tests were run because only documentation changed.
- PASS — targeted `git diff --check` (only expected LF/CRLF notices).

## Current state

- No code or numerical behavior changed.
- Step 2 and Step 3 remain complete.
- Step 8A is a future baseline validation audit before Step 9 interpretation;
  its mesh-dependence checks belong to Step 13.

## Exact next implementation step

The exact next implementation step remains `steps2.md` Step 4. Execute Step 4
only after the user says next.

STOP; Step 4 was not started.
