# ADR01 steps2 Step 1 reopened — remaining documentation drift repaired

## Work completed

- Corrected the handoff-first path in root `steps2.md` from the nonexistent
  `run/01_ADR01/steps2.md` to `steps2.md`.
- Added semantic `by_region` IC to the root README's Test 07 description.
- Replaced the manual's read-the-entire-manual guidance with handoff-first,
  inspect-on-demand guidance when a case already has instructions or a roadmap.
- Updated the transient-model API entry: `previous=None` now documents
  `uniform`, backward-compatible `split_x`, and semantic constant `by_region`.
- Clarified the feature-status limitation: semantic constant `by_region` is
  supported; arbitrary coordinate/expression initial fields are not.
- Refined `steps2.md` contact-resistance language to distinguish supported
  perfect shared-mesh contact, supported steady mesh-resolved thin layers,
  reference-only 1D zero-thickness contact, and future generic 3D/transient
  interface resistance. ADR01 Baseline v1 ordinary contacts remain perfect.

The following requested items were already corrected in the current workspace and
were verified without further edits: root README's general `by_region` capability,
manual heat-switch generic/case-proxy distinction, ADR01 architecture present-tense
data flow and heat-switch implementation, and the implemented-proxy wording in
ADR01 `steps.md`.

## Files changed in this repair

- `steps2.md`
- `README.md`
- `manual/README.md`
- `manual/08-api-reference.md`
- `manual/07-feature-status.md`
- `chat/019.md`

## Key decisions

- Step 1 remains `STATUS: COMPLETE` after the targeted stale-text check passed.
- Contact-resistance documentation describes capability by model/path instead of
  incorrectly claiming that the repository has no contact-resistance support.
- Existing Step 2 code/documentation changes from the preceding work session were
  neither extended nor reverted during this documentation-only repair.

## Tests and checks

- No physics, Python code, or tests were touched or run.
- Targeted stale-text search returned no matches for the reported drift.
- `git diff --check` passed with only expected LF/CRLF notices.

## Blockers / state note

None for Step 1. The working tree already contains Step 2 implementation from the
preceding session and `steps2.md` currently marks Step 2 complete; this repair did
not execute Step 2.

## Exact next step

According to the current working tree, the next incomplete item is `steps2.md`
Step 3 — Separate solver timestep from output cadence. If the intended branch or
push should exclude the pre-existing Step 2 work, reconcile that state explicitly
before continuing.

STOP after the Step 1 documentation repair.
