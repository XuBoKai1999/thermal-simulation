# Repository handoff documentation refresh

已讀取並盤點整個 repository（原始碼、Test 01–07、NIST materials、manual、ADR01 planning、
既有輸出與 Git 狀態），並依 `AGENTS.md` 更新交接性文件：

- 建立 `handoff.md`：記錄目前目標、實作狀態、設計決策、風險、資料缺口、下一步與續接入口。
- 將 `steps.md` 從已完成階段的長篇施工紀錄收斂為目前可執行 roadmap。
- 更新 `arch.md`：採用實際 `run/` layout，補上 `materials/nist/` 的資料責任與尚未 solver-integrated 的邊界。
- 更新 `manual/README.md` 與 `manual/07-feature-status.md`：加入 NIST listing 用法、series qualifiers、有效範圍、density 缺口與 ingestion 統計。
- 更新 ADR01 readiness audit 中已解決的 `run/`/`runs/` 命名差異。

關鍵結論：框架與 NIST ingestion 已完成，但 ADR01 尚未實作；下一個最小工作是把明確選定的
NIST property series 接到既有 `Property` system。四個初始材料皆缺 NIST density，且除了
Aluminum 6061-T6 的 `k` 外，多數 `k/cp` equations 不覆蓋 1–4 K 全域；不得 silent
extrapolation。OFHC copper 還必須指定 RRR，G-10 必須指定 direction。

驗證：透過 `scripts/wsl-run.ps1` 在 WSL2 直接執行兩個 unittest 腳本，共 14 tests 全部通過。
WSL 環境未安裝 pytest，因此未使用 `python3 -m pytest`。

