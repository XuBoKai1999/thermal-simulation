# ADR01 checkpoint filename collision fix

## Completed

- Changed the existing ADR01 runner to name final checkpoints by absolute time:
  `checkpoint_t_<end-time>.npz`.
- Kept the existing output-directory convention; no run manager or new output
  abstraction was added.
- Updated the ADR01 usage documentation and current handoff to reference the
  time-qualified checkpoint.
- Rebuilt the validated `0→0.05 s`, `dt=0.00025 s` source run and permanently
  preserved its common restart state as
  `run/01_ADR01/output/dt_0.00025/checkpoint_t_0.05.npz`.
- Removed the obsolete generated `checkpoint_final.npz` from that directory so
  it cannot be mistaken for the canonical `t=0.05 s` state.

## Files changed

- `run/01_ADR01/main.py` — time-qualified checkpoint filename.
- `run/01_ADR01/README.md` — naming behavior and corrected restart example.
- `handoff.md` — authoritative canonical checkpoint path and collision status.
- `chat/030.md` — this record.

No physics, solver formulation, material, mesh, IC, or BC changed.

## Targeted verification

- WSL `python3 -m py_compile run/01_ADR01/main.py` — PASS.
- WSL baseline regeneration:
  `python3 run/01_ADR01/main.py --dt 0.00025 --end 0.05 --output-every 0.05` —
  PASS; 200 steps, 1043 Newton iterations.
- Checkpoint inspection — PASS: format version 1, `time_s=0.05`, 2506 P1 DOFs,
  all finite, temperature range `[1,4] K`.
- The `dt_0.00025` directory contains `checkpoint_t_0.05.npz` and no longer
  contains the ambiguous `checkpoint_final.npz`.

## Current state

Checkpoint save/load and split-run trajectory equivalence remain verified. The
canonical `t=0.05 s` state now has a stable, unambiguous filename. A later run to
another end time in the same dt directory will write a different checkpoint
filename rather than replace this state.

The broader generated-output Git policy and a future case/physics fingerprint
remain deferred. No branch convergence or 420-second run was started.

## Exact next action

When requested, run the offset-time `dt=0.1`, `0.05`, and `0.025 s` branch study
from `checkpoint_t_0.05.npz`.
