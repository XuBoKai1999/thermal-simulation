# ADR01 approved smoke-run inputs

Current placeholder geometry is human-approved for the Baseline v1 numerical
smoke run only; actual hardware dimensions remain unverified. Step 6 is complete:
`ggg` starts at 1 K as a finite-heat-capacity body, every other solved solid starts
at 4 K, hot-reservoir contacts are fixed at 4 K, other exposed boundaries are
adiabatic, and ordinary contacts remain perfect.

`run/01_ADR01/steps.md` and `arch.md` now record these decisions. Step 9 is blocked
only by solver-facing implementation and property validation. The frozen canonical
Baseline v1 document, material map, geometry, and evidence archive are untouched.

Exact next step: inspect the existing material/property, case, IC, tagged-facet BC,
solver, analysis, and closest verification paths on demand; implement only missing
capabilities.
