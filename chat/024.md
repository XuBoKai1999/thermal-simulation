# Step 4 complete — ParaView-native ADR01 time series

## Completed

- Added a ParaView-native VTK/PVD time series for ADR01 alongside the existing
  text dump pipeline.
- The series starts at the existing `t = 0` initial snapshot and uses the same
  output cadence as subsequent solved snapshots.
- Exported the actual continuous P1 `temperature` field as point data and the
  DG0 `heat_flux` and `region_ID` fields as cell data.
- Kept visualization output isolated under the current run directory and within
  the existing safe generated-output cleanup.
- Documented the output path, field meanings, and ParaView workflow.

## Files changed

- `lib/analyze.py` — names and exposes FEM field functions for native output.
- `run/01_ADR01/main.py` — writes `visualization/fields.pvd` and its VTK pieces.
- `test/07_3d_multiregion_transient/main.py` — targeted field-name/API check.
- `manual/04-output-and-postprocessing.md` — ParaView usage and field roles.
- `manual/07-feature-status.md` — records the implemented native time series.
- `manual/08-api-reference.md` — documents returned field functions.
- `arch.md` — records separation of dump and visualization pipelines.
- `run/01_ADR01/README.md` — gives the case-local ParaView entry point.
- `steps2.md` — marks Step 4 complete.

## Key decisions

- Use the FEniCSx native `VTKFile`/PVD path: it directly supports the actual P1
  temperature together with DG0 cell fields and requires no new abstraction.
- Do not replace or reinterpret the ADR dump format; native visualization is a
  separate output intended for interactive inspection.
- Let `write_function` emit the mesh with each frame. A separate mesh write was
  rejected because it produced a duplicate mesh-only `t = 0` dataset.

## Tests

- `python3 test/07_3d_multiregion_transient/main.py` via WSL — PASS.
- Short ADR01 run, `--dt 0.00015 --end 0.0003 --output-every 0.0003` via WSL —
  PASS; two solver steps and snapshots at `t = 0` and `t = 0.0003`.
- ParaView 6.1.1 `pvpython` opened `fields.pvd` — PASS; times `[0.0, 0.0003]`,
  point field `temperature`, cell fields `heat_flux` and `region_ID`.
- WSL `python3 -m py_compile` for the three modified Python files — PASS.
- Targeted `git diff --check` — PASS (only existing line-ending notices).

## Current state

`steps2.md` Step 4 is complete. The generated verification series is at
`run/01_ADR01/output/dt_0.00015/visualization/fields.pvd`.

Blockers: none.

## Exact next step

Execute `steps2.md` Step 5 only after the user says next.
