# ADR01 steps2 Step 3 complete — solver dt and output cadence separated

## Work completed

- Added optional generic `output.every_time_s` case configuration and validation.
- Added `lib.case.output_timesteps(case_data)` to map uniform requested physical
  times to the nearest actual solver steps.
- Nearest-step behavior records actual solver time, resolves half-step ties toward
  the later step, removes duplicate mapped steps, and does not interpolate.
- Replaced ADR01's three hard-coded observation times with the generic cadence.
- Configured ADR01 visualization smoke output at `every_time_s=0.0025 s`, separate
  from the selected solver `dt_s=0.00025 s`.
- Ran the complete existing 0.05 s ADR01 early-transient interval and produced 20
  snapshots at steps 10, 20, ..., 200 (times 0.0025, 0.005, ..., 0.05 s).
- Marked `steps2.md` Step 3 complete.

## Exact cadence behavior

For requested time `n * output.every_time_s`, choose the nearest actual solver
step. A half-step tie chooses the later step. Several requests mapping to the same
step produce one snapshot. The dump `TIME` is `step * dt_s`, not the nominal
requested time. No temporal interpolation is performed.

ADR01 proof run:

- solver dt: `0.00025 s`;
- output interval: `0.0025 s`;
- end time: `0.05 s`;
- solver steps: 200;
- output snapshots: 20;
- nonlinear iterations: 1043;
- first/last dump: `10.dump` at 0.0025 s / `200.dump` at 0.05 s.

## Files changed

- `lib/case.py`
- `run/01_ADR01/case.yaml`
- `run/01_ADR01/main.py`
- `test/07_3d_multiregion_transient/main.py`
- `manual/02-workflow-geometry-mesh-case.md`
- `manual/04-output-and-postprocessing.md`
- `manual/08-api-reference.md`
- `arch.md`
- `run/01_ADR01/README.md`
- `steps2.md`
- `run/01_ADR01/output/dt_0.00025/dump/` (20 generated v2 dumps)
- `run/01_ADR01/output/dt_0.00025/summary.json`
- `chat/020.md`

## Key decisions

- Implemented only one uniform physical-time interval; no scheduler DSL.
- Used nearest actual solver steps because the current integrator has no temporal
  interpolation path.
- Kept initial-state dumping caller-controlled; this cadence schedules positive
  requested times through `end_s`.

## Tests and checks

- PASS: Test 07 3D multiregion transient. Its non-coincident cadence check uses
  `dt=1 s`, `every_time=1.6 s`, and verifies mapped steps `[2, 3, 5]`.
- PASS: ADR01 `dt=0.00025 s`, `end=0.05 s` run; 1043 Newton iterations.
- PASS: WSL `py_compile` for `lib/case.py`, ADR01 runner, and Test 07.
- PASS: generated summary/dump count and cadence inspection.
- PASS: targeted `git diff --check` (only expected LF/CRLF notices).
- One optional WSL reader one-liner did not start because of nested PowerShell
  quoting; equivalent read-only verification used `summary.json` and dump headers.

## Blockers

None.

## Exact next step

`steps2.md` Step 4 — Add ParaView-native time-series output using the simplest
format naturally supported by the current FEniCSx stack, preserve the FEM
temperature field, verify a short ADR01 series opens correctly, document how to
open it, then STOP.

STOP after Step 3; do not begin Step 4 without explicit user instruction.
