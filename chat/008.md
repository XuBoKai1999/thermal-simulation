# ADR01 Draft 0 geometry completed

Step 0 has been updated with the human-approved decisions and marked `STATUS: COMPLETE`.
Steps 1–3 are complete for Draft 0; Step 4 is explicitly `AWAITING HUMAN VISUAL APPROVAL`.

The new case-local `run/01_ADR01/geometry.py` translates the soft requirements directly into
Gmsh without introducing a DSL or parent-framework abstraction. It generates ten tagged,
conformal volumes and eleven tagged shared contact surfaces. Requirement millimetres are converted
to metres in the mesh.

Automated validation passed:

- all 10 component IDs are present;
- all 11 edge IDs correspond to actual shared surfaces;
- no declared contact is missing;
- no extra component contact was found;
- no split/overlapping component volume was found after OCC fragmentation;
- Gmsh coherence check found no duplicate nodes/elements or isolated nodes;
- mesh contains 2,463 nodes and 7,283 tagged elements.

The generated interactive artifact is `run/01_ADR01/build/mesh.msh`. Open it with:

```powershell
.\scripts\wsl-run.ps1 "cd '/mnt/c/Users/User2/Documents/Works/03 thermal simulation' && python3 run/01_ADR01/geometry.py --view"
```

`run/01_ADR01/geometry-report.md` records contact areas, interpretations, assumptions, and the
inspection procedure. All dimensions and derived areas remain visualization placeholders.

No materials were researched, no thermal case/runner was created, and no thermal solver was run.
Human visual approval remains mandatory before continuing.
