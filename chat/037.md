# Swap-file cleanup and completed baseline handoff

Confirmed `.gitignore.swp` is already absent from both the working tree and Git
index, then added the minimal `*.swp` ignore rule so editor swap files cannot be
reintroduced.

Updated only the stale status statements in `handoff.md`: `baseline-v1` has
completed 0–420 s; S0 is the accepted baseline; S1–S4 pass the current
provisional timestep validation; `validated_through_s = 420`; scenario-level
output exists under `run/01_ADR01/output/baseline-v1/`; and shared runtime
logging is complete. The next planned direction is long-time continuation from
`output/baseline-v1/checkpoint/t_420s.npz`, not another 0–420 s run.

No simulation code was changed and no simulation was executed.
