# ADR01 first transient attempt: pre-solve compile failure

Test 06 and Test 07 pass, including the new region-wise IC path. The first ADR01
dt=1e-3 attempt built/loaded the approved 2 mm placeholder mesh (2506 nodes), then
failed before SNES while FFCx expanded the complete 501-row NIST G-10 cp source
table: NumPy attempted a 256 GiB `(185193, 185193)` allocation. No transient result
was produced; this is a table-expression compilation limitation, not convergence.

The minimal correction is to keep table representation but use the canonical
1–4 K low-temperature checkpoint subset in a case-local mass-specific cp table,
with a 4 K endpoint interpolated from the preserved complete NIST source. The full
source remains untouched and validation continues to reconstruct its volumetric
values. No new interpolation system is being introduced.

Exact next step: rerun property validation against the reduced solver table, then
retry dt=1e-3 and proceed with 5e-4 and 2.5e-4 only if successful.
