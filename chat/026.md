# Diagnostic smoke run — dt=0.1 s

## Request

Check whether ADR01 can run a short transient with `dt=0.1 s` before considering
a much longer physical interval.

## Run

```text
python3 run/01_ADR01/main.py --dt 0.1 --end 1.0 --output-every 0.1
```

Executed through `scripts/wsl-run.ps1` as required.

## Result

- PASS: 10/10 timesteps converged.
- Wall time: approximately 41 seconds including mesh loading and output.
- Total Newton iterations: 36 (3.6 per step average).
- 11 snapshots were written, including `t=0`.
- Final global range at `t=1 s`: `Tmin=1.0460886 K`, `Tmax=4 K`.
- Final GGG average: `1.0887750 K`.
- Output: `run/01_ADR01/output/dt_0.1/`.

This is only a bounded convergence smoke test. `dt=0.1 s` is 400 times the
validated `0.00025 s` baseline timestep, so the run does not establish temporal
accuracy or authorize engineering interpretation. No 420-second run was started.

## Repository state

No code, physics, mesh, configuration, manual, or roadmap behavior changed.
Root `steps2.md` Steps 1–5 remain complete; the exact planned implementation step
remains Step 6, only after user instruction.
