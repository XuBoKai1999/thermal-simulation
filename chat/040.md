已檢查最近完成的 `baseline-v1-cont-420s-1800s`：dt 2 s production 已跑完 420–1800 s，但 manifest 仍是 `validation_status = NOT_PERFORMED`、`validated_through_s = 420`，確實沒有 dt/2 evidence。

新增 `run/01_ADR01/validate_continuation.py`。它不重跑既有 dt 2 s production；會從原本可信的 t=420 s checkpoint 執行 dt 1 s reference，以 2 s cadence 對齊既有 summary，再呼叫現有 `workflow.compare()` 產生 scalar、endpoint full-P1、sanity 與 provisional-gate evidence。輸出獨立放在 `output/baseline-v1-cont-420s-1800s-dt-validation/`，已存在時拒絕覆寫。比較後才更新 continuation manifest：PASS 時 `validated_through_s = 1800`，FAIL 時維持 420。

本輪沒有啟動 transient simulation。WSL Python compile 與既有 workflow self-test 均 PASS。

啟用指令：

```powershell
.\scripts\wsl-run.ps1 "python3 run/01_ADR01/validate_continuation.py"
```
