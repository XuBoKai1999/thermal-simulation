# ADR01 status review reconciliation

The review was correct that ADR01 has advanced into parameterization / baseline-run preparation
and that root handoff/roadmap state lagged behind. Its proposed arbitrary "very small k" wording
and ON-state implementation were not adopted: the latest approved model is the geometry-calibrated
finite OFF proxy, while ON remains unmodeled. IC and non-hot-side BCs also remain unresolved, so a
thermal run is not yet authorized.

Documentation now records human approval of the current lateral-offset geometry on 2026-09-12.
`handoff.md`, root `steps.md`, ADR01 `steps.md`, and `geometry-report.md` point the next session to
IC/BC decisions, solver-facing property translation, property sanity checks, and only then the
transient baseline. Material research is closed and should not be repeated.

Baseline v1, papers, geometry/code/mesh, material decisions, and physical inputs were untouched.
