# ADR01 handoff and generated-output ignore cleanup

Made only the requested documentation and ignore corrections. In `handoff.md`,
the stale claim that no 5-second comparison existed now states that the 0.5–5 s
S2 comparison is accepted, and `Next exact action` now calls for a clean full
trajectory from `t=0` using `baseline-v1.yaml` after human review.

Added four ADR01-output-scoped `.gitignore` patterns for generated `*.npz`,
`*.dump`, `*.vtu`, and `*.pvtu` artifacts. `manifest.json`, `summary.csv`, plans,
and validation reports remain reviewable and are not ignored. Existing tracked
history was not removed or untracked. `git check-ignore` confirmed the intended
scope. No simulation was run.
