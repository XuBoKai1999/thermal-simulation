# ADR01 roadmap and parameter-map synchronization

The local working tree already contained the completed documentation cleanup that
was missing from the reviewed GitHub `main`: the parameter README was canonical,
the stale research/task Markdown files were absent, and ADR01 README/architecture/
agent instructions were clean.

This pass changed only the remaining discrepancies:

- `run/01_ADR01/parameters-requirement/material-map.yaml` is now explicitly
  `frozen`, records the hot reservoir at 4 K and ideal-open heat-switch
  `G_off_W_K: 0.0`, and identifies RRR=50 only as the copper sensitivity.
- `run/01_ADR01/steps.md` now has current numbered gates: Baseline v1 frozen;
  IC/remaining BC unresolved; property implementation/checks not started; transient
  solve blocked until geometry approval and input decisions.
- root `steps.md` now reflects completed ADR01 material research and points to the
  correct case roadmap path.

Consistency checks found no stale active material decisions or fixed-1 K cold-side
claim. Baseline v1 retained SHA-256
`56EBC76150D62BB5530E974C02C08958D4BEE491D4ACFCB60420A60E3DE42E49` and has no
diff. Paper evidence, code, geometry, mesh, tests, and outputs are untouched.
