# Step 2–3 maintenance complete — initial snapshot and clean run output

## Completed

- ADR01 now writes the already-built initial field through the same analysis/dump
  pipeline before the first solve as `0.dump`, with `TIMESTEP=0`, `TIME=0.0`, and
  the active solver dt. The positive-time generic scheduler is unchanged.
- Before an ADR01 run writes output, it removes only `dump/*.dump` and
  `summary.json` inside that run's exact `output/dt_<dt>/` directory. It does not
  recursively remove the directory or touch another dt, mesh, inputs, or papers.
- Added optional CLI `--output-every` solely to permit a short cadence override;
  the case configuration remains the default.
- Corrected current-format wording from v1 to v2 in the dump module docstring,
  feature inventory, and dump specification. References to format v1 now remain
  only where they describe intentional reader compatibility or unchanged fields.
- Strengthened Test 07 to require and verify all v2 metadata: `FORMAT_VERSION`,
  `TIMESTEP`, `TIME`, `SOLVER_DT`, `CHARACTERISTIC_CELL_SIZE`, `MESH_ID`,
  `NUMBER OF CELLS`, and `BOX BOUNDS`.

## Files changed

- `run/01_ADR01/main.py` — initial snapshot, scoped stale-output cleanup, short-run
  cadence override.
- `lib/dump.py` — v2 docstring wording.
- `test/07_3d_multiregion_transient/main.py` — complete v2 metadata assertions.
- `dump-format.md` — current-format v2 wording.
- `manual/07-feature-status.md` — v2 writer / v1 reader-compatibility status.
- `run/01_ADR01/README.md` — initial output and cleanup behavior.
- `manual/04-output-and-postprocessing.md` — caller-controlled initial snapshot.
- `arch.md` — ADR01 output lifecycle responsibility.
- `run/01_ADR01/output/dt_0.0002/` — generated two-step targeted smoke evidence.
- `chat/021.md` — this handoff.

`steps2.md` was not changed.

## Tests

- PASS — `powershell.exe -NoProfile -ExecutionPolicy Bypass -File
  .\scripts\wsl-run.ps1 "python3 test/07_3d_multiregion_transient/main.py"`
  (v2 read/write, all required metadata, v1 compatibility, uniform cadence).
- PASS — short ADR01 run with `--dt 0.0002 --end 0.0004 --output-every 0.0002`:
  produced unique steps `[0, 1, 2]`, including initial and first solved snapshots.
- PASS — same dt/end rerun with `--output-every 0.0004`: old `1.dump` and injected
  `999.dump` were removed; only `[0, 2]` remained and stale summary was replaced.
- PASS — different-dt isolation: existing `dt_0.00025/dump` remained at 20 files.
- PASS — WSL `python3 -m py_compile` for the modified Python files.
- PASS — targeted `git diff --check` (only expected LF/CRLF notices).

## Current state

steps2.md Step 2 and Step 3 remain complete.
Repository is ready to proceed to Step 4.

## Blockers

None.

## Exact next step

Execute steps2.md Step 4 only after user says next.

STOP; Step 4 was not started.
